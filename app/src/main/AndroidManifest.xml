<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScriptQ">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${ADMOB_APP_ID}"/>

        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/Theme.App.Starting"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ScriptEditorActivity"
            android:label="Add New Script"
            android:exported="false" />

        <activity
            android:name=".ui.PaywallActivity"
            android:label="Go Premium"
            android:exported="false" />

        <activity
            android:name=".ui.SettingsActivity"
            android:label="Settings"
            android:exported="false" />

        <activity
            android:name=".ui.OverlayAccessActivity"
            android:label="Overlay Access"
            android:exported="false" />
            
        <activity
            android:name=".ui.OnboardingActivity"
            android:theme="@style/Theme.ScriptQ"
            android:exported="false" />

        <activity
            android:name=".ui.RemoteControlActivity"
            android:label="Remote Control"
            android:exported="false" />

        <service
            android:name=".ui.FloatingPromptService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_TYPE"
                android:value="The floating teleprompter widget needs to remain active while the user is using other apps (like the Camera app) to record themselves, ensuring smooth script delivery without interruptions." />
        </service>
    </application>

</manifest>
